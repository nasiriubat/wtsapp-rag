fastapi==0.128.8
uvicorn==0.39.0
psycopg[binary]==3.2.13
fastembed==0.7.4
httpx==0.28.1
jinja2==3.1.6
itsdangerous==2.2.0
python-multipart==0.0.20
segno==1.6.6
# Uploaded documents. The extras are the parsers we need and nothing else:
# Docling and unstructured pull PyTorch, which would triple the image.
markitdown[pdf,docx,pptx,xlsx]==0.1.7
